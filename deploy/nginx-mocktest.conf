server {
    listen 80;
    server_name multilevelmocktest.uz www.multilevelmocktest.uz;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name multilevelmocktest.uz www.multilevelmocktest.uz;

    ssl_certificate /etc/letsencrypt/live/multilevelmocktest.uz/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/multilevelmocktest.uz/privkey.pem;

    client_max_body_size 50m;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
    }
}
